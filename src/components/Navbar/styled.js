import styled from 'styled-components'

const Header = styled.header`
  display: flex;
  margin-top: 22px;
  padding: 0 0.5em;
`

const Link = styled.a`
  align-self: flex-start;
`

export { Header, Link }
